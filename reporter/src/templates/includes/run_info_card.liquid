<div class="rounded-xl border bg-card text-card-foreground shadow p-4 md:p-6">
    <div class="flex flex-col lg:flex-row lg:items-center justify-between gap-6">
        <div class="flex flex-wrap items-center gap-6 md:gap-8 flex-1">
            <!-- Project Info -->
            <div class="flex items-center gap-3 min-w-[150px]">
                <div class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
                    <i class="fa-solid fa-rocket text-sm"></i>
                </div>
                <div class="truncate">
                    <p class="text-[9px] text-muted-foreground uppercase font-bold tracking-wider mb-0.5">Project & Release</p>
                    <p class="text-sm font-bold text-foreground truncate">{{ data.report.project | default: "N/A" }} <span class="text-xs font-medium text-muted-foreground ml-1">v{{ data.report.release | default: "0.0.0" }}</span></p>
                </div>
            </div>

            <!-- Cycle Info -->
            <div class="flex items-center gap-3 min-w-[150px]">
                <div class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
                    <i class="fa-solid fa-rotate text-sm"></i>
                </div>
                <div class="truncate">
                    <p class="text-[9px] text-muted-foreground uppercase font-bold tracking-wider mb-0.5">Test Cycle</p>
                    <p class="text-sm font-bold text-foreground truncate">{{ data.report.cycle | default: "N/A" }}</p>
                </div>
            </div>

            <!-- Execution Period -->
            <div class="flex items-center gap-3 min-w-[200px]">
                <div class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
                    <i class="fa-solid fa-clock text-sm"></i>
                </div>
                <div class="truncate text-xs">
                    <p class="text-[9px] text-muted-foreground uppercase font-bold tracking-wider mb-0.5">Execution Period</p>
                    <div class="font-medium text-foreground flex items-center gap-2">
                        <span>{{ data.report.executionStartTime }}</span>
                        <span class="opacity-40 italic font-normal">to</span>
                        <span>{{ data.report.executionEndTime }}</span>
                    </div>
                </div>
            </div>

            <!-- Custom User Metadata -->
            {% if data.report.metadata %}
                {% for item in data.report.metadata %}
                <div class="flex items-center gap-3 min-w-[120px]">
                    <div class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary">
                        <i class="fa-solid fa-circle-info text-sm"></i>
                    </div>
                    <div class="truncate">
                        <p class="text-[9px] text-muted-foreground uppercase font-bold tracking-wider mb-0.5">{{ item.label }}</p>
                        <p class="text-sm font-bold text-foreground truncate">{{ item.value }}</p>
                    </div>
                </div>
                {% endfor %}
            {% endif %}
        </div>
    </div>
</div>
