name: Test Cypress Example

on:
  push:
    branches:
      - main
    paths:
      - examples/cypress/**
      - reporter/**

  pull_request:
    branches:
      - main
    paths:
      - examples/cypress/**
      - reporter/**

jobs:
  test:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install Monorepo Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Project
        working-directory: reporter
        run: pnpm build

      - name: Install Cypress Binary
        working-directory: examples/cypress
        run: pnpm cypress install

      - name: Run Cypress Tests
        working-directory: examples/cypress
        run: pnpm test
        continue-on-error: true

      - name: Link the CLI Globally
        run: pnpm link --global ./reporter

      - name: Sanity check CLI command
        working-directory: examples/cypress
        run: mchr --version

      - name: Generate Report
        working-directory: examples/cypress
        run: pnpm report

      - name: Upload Report
        uses: actions/upload-artifact@v7
        with:
          name: cypress-report-${{ matrix.os }}
          path: examples/cypress/.run/html-report
