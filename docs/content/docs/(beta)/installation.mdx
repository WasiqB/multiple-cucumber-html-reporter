---
title: Installation
description: Learn how to install and set up Multiple Cucumber HTML Reporter.
icon: Download
---

Multiple Cucumber HTML Reporter can be installed easily using your favorite package manager.

## Install globally

The reporter comes with an in-built command line tool which helps in generating the report without requiring you to write any script to generate the report.

To install globally, run the following command in your terminal:

<Tabs groupId='node' items={['npm', 'yarn', 'pnpm']} persist>
  <Tab value="npm">
  ```bash
  npm install multiple-cucumber-html-reporter --global
  ```
  </Tab>
  <Tab value="yarn">
  ```bash
  yarn global add multiple-cucumber-html-reporter
  ```
  </Tab>
  <Tab value="pnpm">
  ```bash
  pnpm add multiple-cucumber-html-reporter --global
  ```
  </Tab>
</Tabs>

Check out more about the [command line tool](/docs/cli).

## Install Locally

If you want to generate the report programmatically by creating a script, then you need to install the package locally in your project by running the following command in your terminal in your project directory:

<Tabs groupId='node' items={['npm', 'yarn', 'pnpm']} persist>
  <Tab value="npm">
  ```bash
  npm install multiple-cucumber-html-reporter --save-dev
  ```
  </Tab>
  <Tab value="yarn">
  ```bash
  yarn add multiple-cucumber-html-reporter --dev
  ```
  </Tab>
  <Tab value="pnpm">
  ```bash
  pnpm add multiple-cucumber-html-reporter --save-dev
  ```
  </Tab>
</Tabs>

## Compatibility

This reporter is highly compatible with different versions of **CucumberJS**:

- **CucumberJS 1**: Fully supported.
- **CucumberJS 2**: Fully supported.
- **CucumberJS 3**: Supported (requires a separate script).
- **CucumberJS 4**: Fully supported.

<Callout title="Note" type="idea">
For WebdriverIO users, please check our [WebdriverIO Integration Guide](/docs/frameworks/webdriverio) for a simplified setup.
</Callout>
